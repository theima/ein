{
  "name": "glo",
  "version": "0.0.1",
  "description": "",
  "main": "dist/bundle/glo.umd.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "tsc --strict --suppressImplicitAnyIndexErrors",
    "bundle": "webpack",
    "lint": "tslint src/*.ts",
    "clean": "rimraf ./dist",
    "clean:dist-js": "rimraf ./dist/*.js && rimraf ./dist/src/*.js",
    "uglify": "uglifyjs -c -o ./dist/bundle/glo.min.umd.js ./dist/bundle/glo.umd.js",
    "prebuild": "npm run clean && npm run lint",
    "build": "npm run transpile && npm run bundle && npm run uglify",
    "postbuild": "npm run clean:dist-js",
    "prepublishOnly": "npm run build"
  },
  "author": "Emanuel Eriksson",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "peerDependencies": {
    "emce": "^0.9.6"
  },
  "devDependencies": {
    "emce": "^0.9.6",
    "rimraf": "^2.6.2",
    "rxjs": "^5.5.5",
    "snabbdom": "^0.7.0",
    "ts-lint": "^4.5.1",
    "ts-loader": "^3.2.0",
    "typescript": "^2.6.2",
    "uglify-js": "^3.2.2",
    "webpack": "^3.10.0"
  }
}
