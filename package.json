{
  "name": "ein",
  "version": "0.0.1",
  "description": "",
  "main": "dist/bundle/ein.umd.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "karma start",
    "test:watch": "karma start --no-single-run",
    "transpile": "tsc",
    "bundle": "webpack",
    "lint": "tslint -p tsconfig.json 'src/**/*.ts' && tslint -p tsconfig.json 'src/**/*.spec.ts' && markdownlint '*.md'",
    "clean": "rimraf ./dist",
    "clean:dist-js": "rimraf ./dist/*.js && rimraf ./dist/src/*.js",
    "prebuild": "npm run clean && npm run lint && npm run test",
    "build": "npm run transpile && npm run bundle",
    "postbuild": "npm run clean:dist-js",
    "quickbuild": "npm run clean && npm run transpile",
    "prepublishOnly": "npm run build"
  },
  "author": "Emanuel Eriksson",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "peerDependencies": {
    "rxjs": "^6.2.0"
  },
  "devDependencies": {
    "@types/history": "^4.7.5",
    "@types/jasmine": "^3.5.10",
    "array-find": "^1.0.0",
    "array-find-index": "^1.0.2",
    "history": "^4.10.1",
    "jasmine-core": "^3.5.0",
    "karma": "^5.0.1",
    "karma-chrome-launcher": "^3.1.0",
    "karma-jasmine": "^3.1.1",
    "karma-mocha-reporter": "^2.2.2",
    "karma-typescript-preprocessor2": "^1.2.1",
    "karma-webpack": "^4.0.2",
    "markdownlint-cli": "^0.22.0",
    "path-to-regexp": "^6.1.0",
    "rimraf": "^3.0.2",
    "rxjs": "^6.5.5",
    "snabbdom": "^0.7.4",
    "ts-loader": "^6.2.2",
    "tslint": "^6.1.1",
    "tslint-etc": "^1.10.1",
    "typescript": "^3.8.3",
    "webpack": "^4.42.1",
    "webpack-cli": "^3.3.11",
    "webpack-rxjs-externals": "^2.0.0"
  }
}
